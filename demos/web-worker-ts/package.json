{
  "name": "simple-web-worker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "./node_modules/.bin/copyfiles -u 1 src/index.html out && npm run compile",
    "compile": "./node_modules/.bin/tsc -p ./src"
  },
  "author": "",
  "devDependencies": {
    "@types/node": "^12.7.5",
    "copyfiles": "^2.1.1",
    "serve": "^14.2.4",
    "typescript": "^3.6.3"
  }
}
